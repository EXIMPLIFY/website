import Banner from './banner';
import HsnResults from './hsn-results';

const HsnLocator = () => {
  return (
    <main>
      <Banner />
      <HsnResults />
    </main>
  );
};

export default HsnLocator;
